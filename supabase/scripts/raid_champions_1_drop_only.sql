-- Bước A — Mở tab SQL MỚI (New query), KHÔNG dán vào cuối file khác.
-- Chỉ chạy đúng 1 dòng dưới đây → Run.
-- Nếu báo Success, sang file raid_champions_2_create_only.sql (cùng thư mục scripts/)

DROP TABLE IF EXISTS public.raid_champions CASCADE;
