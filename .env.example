VITE_SUPABASE_URL=https://ztlsgxdkcgohkumdaxpi.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_yM69BknwTmBPZjqRvSexTQ_nYXWi-K7

# Bắt buộc tạo bucket trong Supabase (Storage → New bucket) đúng tên bên dưới, không chỉ khai báo trong .env.
# Bucket public + policy INSERT cho authenticated (hoặc theo nhu cầu), nếu không upload ảnh bài viết sẽ báo "Bucket not found".
VITE_SUPABASE_POST_IMAGES_BUCKET=post-images

# Chỉ dùng cục bộ khi chạy: npm run sync:champions (đẩy bảng raid_champions). Không commit key này.
# SUPABASE_SERVICE_ROLE_KEY=

# Tuỳ chọn — bucket mirror portrait (mặc định raid-portraits). Tạo bucket qua migration 20260404150000.
# SUPABASE_RAID_PORTRAITS_BUCKET=raid-portraits